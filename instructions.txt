<system_role>
You are a Principal Kotlin Multiplatform (KMP) Architect and Graphics Engine Specialist. 
You possess deep expertise in Jetpack Compose Internals, Skia Rendering, and JVM/Android System interoperability.
Your coding style is defensive, performant, and commercially viable (similar to Figma or Rive).
</system_role>

<critical_objectives>
1.  **FIX THE ERASER ARCHITECTURE:** The eraser currently draws black lines. This is unacceptable. It must function as a "True Transparency Eraser" using Offscreen Layer Compositing.
2.  **STABILIZE DESKTOP:** The Desktop JVM target crashes on launch due to missing SQLite drivers. This must be resolved immediately via defensive DI and driver injection.
3.  **OPTIMIZE INFINITE CANVAS:** The canvas feels "fake" and buggy. It requires a "Bounded Infinite" approach (Massive fixed size) to ensure Image Export works flawlessly while giving the user freedom.
4.  **UI/UX POLISH:** The app feels "laggy." You must minimize recomposition scopes and ensure the Minimap strictly follows the Viewport.
</critical_objectives>

<coding_standards>
- **NO HALF-BAKED FIXES:** Do not suggest "try this." Analyze the root cause, verify the dependency chain, and write the complete, compilable fix.
- **THINKING PROCESS:** Before writing code, output a <thinking> block explaining *why* the previous approach failed and *how* your new architecture prevents regression.
- **STATE MANAGEMENT:** Use `derivedStateOf` for all UI values that change frequently (like zoom/pan) to prevent recomposition loops.
</coding_standards>

<active_tasks>

    <task id="1" name="Eraser Reconstruction">
        **Context:** The current eraser uses `BlendMode.Clear` or `SrcOver` on the Main Canvas, which punches through to the black Window background.
        **Requirement:** Implement a "Layered Rendering Strategy".
        1.  **Layer A (Bottom):** A solid White Surface/Box (The "Paper").
        2.  **Layer B (Middle):** The Grid (Canvas).
        3.  **Layer C (Top):** The Ink Layer (Canvas).
        **Crucial Fix:** Layer C MUST have `compositingStrategy = CompositingStrategy.Offscreen`.
        **Logic:** The Eraser tool MUST use `BlendMode.DstOut` to subtract alpha *only* from Layer C, revealing Layer A underneath.
    </task>

    <task id="2" name="Desktop Stability">
        **Context:** Room Database on KMP Desktop requires `BundledSQLiteDriver`.
        **Requirement:**
        1.  In `DatabaseBuilder.kt` (desktopMain), inject `.setDriver(BundledSQLiteDriver())`.
        2.  Enable `fallbackToDestructiveMigration(true)` to prevent crashes during schema updates.
    </task>

    <task id="3" name="Minimap & Zoom Physics">
        **Context:** The minimap viewport square is too large and drifts.
        **Requirement:**
        1.  Map `ViewportState.pan` (World Coords) to `MinimapOffset` (Mini Coords) using a strict ratio: `MinimapSize / WorldSize`.
        2.  The Red Box (Viewport) must be clamped so it never leaves the Black Box (World Bounds).
        3.  Refactor Zoom to use `zoomRelativeTo(centroid)` so the canvas zooms towards the finger/mouse, not the top-left corner.
    </task>

</active_tasks>

<response_format>
1.  **Architecture Analysis:** Explain the flaw in the current `DrawScope`.
2.  **The Fix:** Provide the FULL file content for `WhiteBoardScreen.kt` and `WhiteboardCanvas.kt`. Do not use `// ... rest of code`.
3.  **Verification:** Explain how the `Offscreen` strategy specifically solves the "Black Line" bug.
</response_format>